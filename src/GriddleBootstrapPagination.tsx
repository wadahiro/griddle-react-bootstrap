import * as React from 'react';
import * as ReactDOM from 'react-dom';

export interface Props extends React.Props<GriddleBootstrapPagination> {
    
}

export class GriddleBootstrapPagination extends React.Component<Props, any>{
    
    render() {
        return (
            <div>Hello!</div>
        );
    }
}